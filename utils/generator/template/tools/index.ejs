<% Object.entries(pathTree).forEach(([category, toolNames]) => { -%>
import {
<% Object.keys(toolNames).forEach((toolName, index) => { -%>
  <%= toolName %>Tool,
<% }); %>} from "./<%= category %>.js";

export const <%= category %>Tools = [
<% Object.keys(toolNames).forEach((toolName, index) => { -%>
  <%= toolName %>Tool,
<% }); %>];

<% }); %>